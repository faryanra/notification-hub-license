# محتوای جدید vercel.json
@'
{
  "functions": {
    "api/*.php": {
      "runtime": "php@0.0.1"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/public/$1" }
  ]
}
'@ | Out-File -FilePath vercel.json -Encoding UTF8
